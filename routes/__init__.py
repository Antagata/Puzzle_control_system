# routes/__init__.py
# Keep this file minimal; do NOT register here.
 # Intentionally empty to avoid import-time side effects.
 # Blueprints are imported explicitly in app.py during registration.
# Intentionally empty to avoid import-time side effects.
# Blueprints are imported explicitly inside app.register_blueprints().
# __all__ = []
